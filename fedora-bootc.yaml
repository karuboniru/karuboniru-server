releasever: rawhide
variables:
  distro: "fedora"

repos:
  - fedora-rawhide

metadata:
  name: fedora-bootc
  summary: Fedora base bootc image

include:
  - fedora-bootc-base/manifest.yaml
  - fedora-bootc-base/kernel.yaml

